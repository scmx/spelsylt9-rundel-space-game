{
  "name": "spelsylt9-rundel-space-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build-itch": "tsc && mkdir -p itch && DIST=itch/dist vite build --base=./ && cd itch/dist; zip ../dist.zip -r .",
    "build": "tsc && DIST=docs vite build --base=/spelsylt9-rundel-space-game",
    "preview": "DIST=docs vite preview --base=/spelsylt9-rundel-space-game"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "vitest": "^0.34.6"
  }
}
