{
  "name": "spelsylt9-rundel-space-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base=/spelsylt9-rundel-space-game",
    "preview": "vite preview --base=/spelsylt9-rundel-space-game"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "vitest": "^0.34.6"
  }
}
